# 논의7: 번개 버튼 설정으로 변경

**날짜**: 2025-12-16
**결정**: 볼트 스위처 → 설정 화면

---

## 🔄 사용자 피드백

**사용자**: "이것보다는 번개 누르면 차라리 설정이 좋을 것 같아"

**이유**:
- 볼트 스위처보다 설정이 더 유용함
- 볼트 전환은 자주 하는 작업이 아님
- 설정에서 모델 선택, Context Mode 등을 빠르게 변경 가능

---

## 📝 변경 사항

### Before (v0.3.2)
```typescript
switchBtn.addEventListener('click', () => {
  commands.executeCommandById('app:open-vault');
});
```

### After (v0.3.3)
```typescript
switchBtn.addEventListener('click', () => {
  const setting = (this.app as any).setting;
  if (setting && setting.open) {
    setting.open();
    setting.openTabById('vault-council'); // Open our settings tab
  }
});
```

---

## ✅ 다음 작업

1. CouncilView.ts 수정
2. 빌드
3. v0.3.3으로 릴리즈

---
