# 논의1: 현재 이슈 및 개선 방향

**날짜**: 2025-12-16
**버전**: v0.3.1

## 🚨 발견된 이슈

### 1. 볼트 변경 기능 문제
- **현상**: 번개(⚡) 버튼을 누르면 설정창으로만 이동
- **문제점**: 실제로 볼트를 변경하는 기능이 아님
- **현재 구현**: `app.setting.open()` + `openTabById('file')` 사용
- **사용자 기대**: 직접 볼트를 전환할 수 있어야 함

### 2. 핫키 동작 문제
- **현상**:
  1. 다른 파일을 선택
  2. Vault Council 핫키를 누름
  3. 동작하지 않음
  4. 플러그인을 종료하고 다시 핫키를 눌러야 그 파일로 넘어감
- **문제점**: 실시간으로 파일 변경이 반영되지 않음
- **추정 원인**: View가 이미 열려있을 때 컨텍스트 갱신이 안됨

### 3. 미구현 기능
- **Custom Context Mode**: 여러 파일을 수동으로 선택해서 분석
- **현재 상태**:
  - Auto mode ✅ (현재 파일 + 링크)
  - Folder mode ✅ (폴더 전체 파일)
  - Custom mode ❌ (여러 파일 수동 선택 - placeholder만 있음)

---

## 💡 개선 방향

### 1. 볼트 변경 기능 개선안

#### 옵션 A: Command Palette 열기
```typescript
// Command palette를 열어서 사용자가 직접 검색하게
app.commands.executeCommandById('command-palette:open');
```

#### 옵션 B: Vault 목록 표시 (Modal)
```typescript
// 사용 가능한 볼트 목록을 Modal로 표시
// 사용자가 선택하면 해당 볼트로 전환
```

#### 옵션 C: 버튼 제거 또는 변경
```typescript
// 볼트 이름만 표시 (클릭 불가)
// 또는 현재 볼트 정보만 보여주는 info 버튼으로 변경
```

**추천**: 옵션 C - 실제 볼트 전환은 Obsidian 네이티브 기능 사용 권장

---

### 2. 핫키 동작 문제 해결

#### 문제 분석
- Sidebar view가 이미 열려있을 때 새로운 파일 컨텍스트가 자동 갱신 안됨
- 사용자가 핫키를 누를 때마다 현재 활성 파일을 다시 읽어야 함

#### 해결 방안
```typescript
// CouncilView에 refresh 메서드 추가
async refreshContext() {
  this.contextEl.empty();
  const context = await this.gatherContext();
  this.displayContext(context);
}

// Plugin에서 핫키 실행 시 refresh 호출
this.addCommand({
  id: 'open-vault-council',
  name: 'Open Vault Council',
  callback: async () => {
    await this.activateView();
    // View가 이미 열려있으면 컨텍스트 갱신
    const view = this.app.workspace.getLeavesOfType(VIEW_TYPE_COUNCIL)[0]?.view;
    if (view instanceof CouncilView) {
      await view.refreshContext();
    }
  }
});
```

---

### 3. Custom Mode 구현

#### 필요한 UI
- 파일 선택 Modal
- 선택된 파일 목록 표시
- 파일 추가/제거 버튼

#### 구현 단계
1. **SuggestModal 생성**: Obsidian의 파일 검색 UI 사용
2. **선택된 파일 저장**: CouncilView에 selectedFiles 배열 관리
3. **컨텍스트 수집**: 선택된 파일들의 내용 읽기
4. **UI 표시**: 현재 선택된 파일 목록 보여주기

```typescript
// 예시 코드
class FileSelectorModal extends SuggestModal<TFile> {
  onChooseSuggestion(file: TFile) {
    this.view.addSelectedFile(file);
  }
}

// CouncilView에 추가
selectedFiles: TFile[] = [];

addSelectedFile(file: TFile) {
  if (!this.selectedFiles.includes(file)) {
    this.selectedFiles.push(file);
    this.refreshContext();
  }
}
```

---

## 🎯 우선순위

### High Priority (즉시 수정)
1. ✅ **핫키 동작 문제** - 사용성에 직접적 영향
2. ✅ **볼트 변경 버튼** - 혼란을 줄 수 있음

### Medium Priority (다음 버전)
3. ⏳ **Custom Mode 구현** - 기능 완성도

### Low Priority (선택 사항)
4. 🔵 기타 UI 개선사항

---

## 📝 다음 액션

1. 볼트 변경 버튼 처리 방향 결정
2. 핫키 동작 문제 수정 구현
3. (선택) Custom Mode 구현 여부 결정

---

**논의 계속**: 다음 논의2에서...
